# servmonitor
Lightweight Server Monitoring tool for Linux/Unix and Windows

How to Use: 
For Linux/macOS: Save the Bash script to a file (monitor.sh), make it executable (chmod +x monitor.sh), and then run it (./monitor.sh).
For Windows: Save the PowerShell script as a .ps1 file and execute it in a PowerShell window with Administrator privileges.
